@model List<TeacherIndexVM>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Teacher List</h4>
                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                <tr>
                                    <th>
                                        Id
                                    </th>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        AcademicRanking
                                    </th>
                                    <th>
                                        Image
                                    </th>
                                    <th>
                                        <a class="btn btn-success" asp-controller="Teacher" asp-action="Create">Create</a>
                                    </th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var teacher in Model)
                                {
                                    <tr class="table-dark">
                                        <td>
                                            @teacher.Id
                                        </td>
                                        <td>
                                            @teacher.Name
                                        </td>
                                        <td>
                                            @teacher.AcademicRanking
                                        </td>
                                        <td>
                                            <img src="~/assets/img/teacher/@teacher.Image" alt="@teacher.Image" style="width: 200px; height: 200px">
                                        </td>
                                        <td>
                                            <a class="btn btn-primary" asp-controller="Teacher" asp-action="Detail" asp-route-id="@teacher.Id">Detail</a>
                                            <a class="btn btn-warning" asp-controller="Teacher" asp-action="Update" asp-route-id="@teacher.Id">Update</a>
                                            <a class="btn btn-danger" id="DeleteCat" asp-controller="Teacher" asp-action="Delete" asp-route-id="@teacher.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
