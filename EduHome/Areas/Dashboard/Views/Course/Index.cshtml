@model List<CourseListVM>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}


<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Courses List</h4>
                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                <tr>
                                    <th>
                                        Id
                                    </th>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        Image
                                    </th>
                                    <th>
                                        <a class="btn btn-success" asp-controller="Course" asp-action="Create">Create</a>
                                    </th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var course in Model)
                                {
                                    <tr class="table-dark">
                                        <td>
                                            @course.Id
                                        </td>
                                        <td>
                                            @course.Name
                                        </td>
                                        <td>
                                            <img src="~/assets/img/course/@course.Image" alt="@course.Image" style="width: 200px; height: 200px">
                                        </td>
                                        <td>
                                            <a class="btn btn-primary" asp-controller="Course" asp-action="Detail" asp-route-id="@course.Id">Detail</a>
                                            <a class="btn btn-warning" asp-controller="Course" asp-action="Update" asp-route-id="@course.Id">Update</a>
                                            <a class="btn btn-danger" id="DeleteCat" asp-controller="Course" asp-action="Delete" asp-route-id="@course.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


