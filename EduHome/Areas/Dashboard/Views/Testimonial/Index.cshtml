@model List<Testimonial>

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-lg-12 stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Category List</h4>
                        <div class="table-responsive pt-3">
                            <table class="table table-bordered">
                                <thead>
                                <tr>
                                    <th>
                                        id
                                    </th>
                                    <th>
                                        Image
                                    </th>
                                    <th>
                                        Name
                                    </th>
                                    <th>
                                        <a class="btn btn-success" asp-controller="Testimonial" asp-action="Create">Create</a>
                                    </th>
                                </tr>
                                </thead>
                                <tbody>
                                @foreach (var slider in Model)
                                {
                                    <tr class="table-dark">
                                        <td>
                                            @slider.Id
                                        </td>
                                        <td>
                                            <img src="~/assets/img/testimonial/@slider.Image" alt="testimonial">
                                        </td>   
                                        <td>
                                            @slider.Name
                                        </td>
                                        <td>
                                            <a class="btn btn-primary" asp-controller="Testimonial" asp-action="Detail" asp-route-id="@slider.Id">Detail</a>
                                            <a class="btn btn-warning" asp-controller="Testimonial" asp-action="Update" asp-route-id="@slider.Id">Update</a>
                                            <a class="btn btn-danger" id="DeleteCat" asp-controller="Testimonial" asp-action="Delete" asp-route-id="@slider.Id">Delete</a>
                                        </td>
                                    </tr>
                                }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
